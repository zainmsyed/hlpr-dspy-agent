[project]
name = "hlpr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dspy-ai>=3.0.3",
    "rich>=14.1.0",
    "typer>=0.17.3",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
